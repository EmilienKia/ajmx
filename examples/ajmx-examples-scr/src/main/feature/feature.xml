<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.6.0" name="${project.artifactId}">

    <repository>mvn:com.github.emilienkia.ajmx/ajmx-osgi/${project.version}/xml/features</repository>

    <feature name="${project.artifactId}" description="${project.description}" version="${project.version}">
        <feature dependency="true" prerequisite="true">ajmx-osgi</feature>
        <feature dependency="true" prerequisite="true">scr</feature>
    </feature>
</features>